// Inbound webhook primitives (verify / secret / parse / dedupe + provider presets).
export * from './inbound'
// Outbound webhook sender types + service.
export * from './types'
export { WebhookService } from './webhook-service'
