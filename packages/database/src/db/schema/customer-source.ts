// packages/database/src/db/schema/customer-source.ts
// Drizzle table: customerSource (generated by scripts/split-schema.ts)

import {
  pgTable,
  uniqueIndex,
  index,
  text,
  timestamp,
  jsonb,
  type AnyPgColumn,
  customerSourceType,
} from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Contact } from './contact'
import { Organization } from './organization'

/** Drizzle table for customerSource */
export const CustomerSource = pgTable(
  'CustomerSource',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
    source: customerSourceType().default('EMAIL').notNull(),
    sourceId: text().notNull(),
    email: text(),
    sourceData: jsonb(),
    organizationId: text()
      .notNull()
      .references((): AnyPgColumn => Organization.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    contactId: text()
      .notNull()
      .references((): AnyPgColumn => Contact.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
  },
  (table) => [
    index('CustomerSource_contactId_idx').using('btree', table.contactId.asc().nullsLast()),
    index('CustomerSource_email_idx').using('btree', table.email.asc().nullsLast()),
    index('CustomerSource_organizationId_idx').using(
      'btree',
      table.organizationId.asc().nullsLast()
    ),
    uniqueIndex('CustomerSource_source_sourceId_organizationId_key').using(
      'btree',
      table.source.asc().nullsLast(),
      table.sourceId.asc().nullsLast(),
      table.organizationId.asc().nullsLast()
    ),
  ]
)
