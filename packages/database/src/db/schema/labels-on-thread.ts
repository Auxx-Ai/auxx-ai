// packages/database/src/db/schema/labels-on-thread.ts
// Drizzle table: labelsOnThread (generated by scripts/split-schema.ts)

import { type AnyPgColumn, pgTable, primaryKey, text } from './_shared'

import { Label } from './label'
import { Thread } from './thread'

/** Drizzle table for labelsOnThread */
export const LabelsOnThread = pgTable(
  'LabelsOnThread',
  {
    threadId: text()
      .notNull()
      .references((): AnyPgColumn => Thread.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    labelId: text()
      .notNull()
      .references((): AnyPgColumn => Label.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
  },
  (table) => [primaryKey({ columns: [table.threadId, table.labelId], name: 'LabelsOnThread_pkey' })]
)
