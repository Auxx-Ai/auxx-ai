// packages/database/src/db/schema/inbox-member-access.ts
// Drizzle table: inboxMemberAccess (generated by scripts/split-schema.ts)

import { pgTable, uniqueIndex, index, text, timestamp, type AnyPgColumn } from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Inbox } from './inbox'
import { OrganizationMember } from './organization-member'

/** Drizzle table for inboxMemberAccess */
export const InboxMemberAccess = pgTable(
  'InboxMemberAccess',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
    inboxId: text()
      .notNull()
      .references((): AnyPgColumn => Inbox.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    organizationMemberId: text()
      .notNull()
      .references((): AnyPgColumn => OrganizationMember.id, {
        onUpdate: 'cascade',
        onDelete: 'cascade',
      }),
  },
  (table) => [
    index('InboxMemberAccess_inboxId_idx').using('btree', table.inboxId.asc().nullsLast()),
    uniqueIndex('InboxMemberAccess_inboxId_organizationMemberId_key').using(
      'btree',
      table.inboxId.asc().nullsLast(),
      table.organizationMemberId.asc().nullsLast()
    ),
    index('InboxMemberAccess_organizationMemberId_idx').using(
      'btree',
      table.organizationMemberId.asc().nullsLast()
    ),
  ]
)
