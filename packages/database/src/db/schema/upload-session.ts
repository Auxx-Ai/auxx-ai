// packages/database/src/db/schema/upload-session.ts
// Drizzle table: uploadSession (generated by scripts/split-schema.ts)

import {
  pgTable,
  index,
  text,
  bigint,
  timestamp,
  jsonb,
  type AnyPgColumn,
  storageProvider,
} from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Organization } from './organization'

/** Drizzle table for uploadSession */
export const UploadSession = pgTable(
  'UploadSession',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    organizationId: text()
      .notNull()
      .references((): AnyPgColumn => Organization.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    provider: storageProvider().default('S3').notNull(),
    externalId: text(),
    credentialId: text(),
    fileName: text().notNull(),
    mimeType: text(),
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    expectedSize: bigint({ mode: 'number' }),
    checksum: text(),
    createdById: text().notNull(),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
    completedAt: timestamp({ precision: 3 }),
    canceledAt: timestamp({ precision: 3 }),
    metadata: jsonb(),
  },
  (table) => [
    index('UploadSession_organizationId_createdById_idx').using(
      'btree',
      table.organizationId.asc().nullsLast(),
      table.createdById.asc().nullsLast()
    ),
    index('UploadSession_provider_externalId_idx').using(
      'btree',
      table.provider.asc().nullsLast(),
      table.externalId.asc().nullsLast()
    ),
  ]
)
