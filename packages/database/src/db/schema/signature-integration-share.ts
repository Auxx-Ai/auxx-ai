// packages/database/src/db/schema/signature-integration-share.ts
// Drizzle table: signatureIntegrationShare (generated by scripts/split-schema.ts)

import { pgTable, uniqueIndex, index, text, timestamp, type AnyPgColumn } from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Integration } from './integration'
import { Signature } from './signature'

/** Drizzle table for signatureIntegrationShare */
export const SignatureIntegrationShare = pgTable(
  'SignatureIntegrationShare',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    signatureId: text()
      .notNull()
      .references((): AnyPgColumn => Signature.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    integrationId: text()
      .notNull()
      .references((): AnyPgColumn => Integration.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
  },
  (table) => [
    index('SignatureIntegrationShare_integrationId_idx').using(
      'btree',
      table.integrationId.asc().nullsLast()
    ),
    index('SignatureIntegrationShare_signatureId_idx').using(
      'btree',
      table.signatureId.asc().nullsLast()
    ),
    uniqueIndex('SignatureIntegrationShare_signatureId_integrationId_key').using(
      'btree',
      table.signatureId.asc().nullsLast(),
      table.integrationId.asc().nullsLast()
    ),
  ]
)
