// packages/database/src/db/schema/integration-tag-label.ts
// Drizzle table: integrationTagLabel (generated by scripts/split-schema.ts)

import { pgTable, uniqueIndex, index, text, timestamp, type AnyPgColumn } from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Label } from './label'
import { Organization } from './organization'
import { Tag } from './tag'

/** Drizzle table for integrationTagLabel */
export const IntegrationTagLabel = pgTable(
  'IntegrationTagLabel',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
    tagId: text()
      .notNull()
      .references((): AnyPgColumn => Tag.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    labelId: text()
      .notNull()
      .references((): AnyPgColumn => Label.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    integrationId: text().notNull(),
    organizationId: text()
      .notNull()
      .references((): AnyPgColumn => Organization.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
  },
  (table) => [
    index('IntegrationTagLabel_integrationId_idx').using(
      'btree',
      table.integrationId.asc().nullsLast()
    ),
    uniqueIndex('IntegrationTagLabel_integrationId_labelId_key').using(
      'btree',
      table.integrationId.asc().nullsLast(),
      table.labelId.asc().nullsLast()
    ),
    uniqueIndex('IntegrationTagLabel_integrationId_tagId_key').using(
      'btree',
      table.integrationId.asc().nullsLast(),
      table.tagId.asc().nullsLast()
    ),
    index('IntegrationTagLabel_organizationId_idx').using(
      'btree',
      table.organizationId.asc().nullsLast()
    ),
  ]
)
