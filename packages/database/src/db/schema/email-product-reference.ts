// packages/database/src/db/schema/email-product-reference.ts
// Drizzle table: emailProductReference (generated by scripts/split-schema.ts)

import {
  pgTable,
  uniqueIndex,
  text,
  bigint,
  boolean,
  doublePrecision,
  type AnyPgColumn,
} from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Message } from './message'
import { Product } from './product'

/** Drizzle table for emailProductReference */
export const EmailProductReference = pgTable(
  'EmailProductReference',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    messageId: text()
      .notNull()
      .references((): AnyPgColumn => Message.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    productId: bigint({ mode: 'number' })
      .notNull()
      .references((): AnyPgColumn => Product.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    inventoryAvailable: boolean().default(true).notNull(),
    issueCategory: text(),
    mentionType: text(),
    returnRate: doublePrecision(),
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    variantId: bigint({ mode: 'number' }),
  },
  (table) => [
    uniqueIndex('EmailProductReference_messageId_productId_key').using(
      'btree',
      table.messageId.asc().nullsLast(),
      table.productId.asc().nullsLast()
    ),
  ]
)
