// packages/database/src/db/schema/order-return.ts
// Drizzle table: orderReturn (generated by scripts/split-schema.ts)

import { pgTable, index, bigint, timestamp, type AnyPgColumn, returnStatus } from './_shared'

import { Order } from './order'

/** Drizzle table for orderReturn */
export const OrderReturn = pgTable(
  'OrderReturn',
  {
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    id: bigint({ mode: 'number' }).primaryKey().notNull(),
    createdAt: timestamp({ precision: 3 }).notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
    status: returnStatus().notNull(),
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    orderId: bigint({ mode: 'number' })
      .notNull()
      .references((): AnyPgColumn => Order.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
  },
  (table) => [index('OrderReturn_orderId_idx').using('btree', table.orderId.asc().nullsLast())]
)
