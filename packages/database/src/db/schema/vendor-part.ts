// packages/database/src/db/schema/vendor-part.ts
// Drizzle table: vendorPart (generated by scripts/split-schema.ts)

import {
  pgTable,
  uniqueIndex,
  text,
  boolean,
  timestamp,
  integer,
  type AnyPgColumn,
} from './_shared'
import { createId } from '@paralleldrive/cuid2'

import { Contact } from './contact'
import { Organization } from './organization'
import { Part } from './part'

/** Drizzle table for vendorPart */
export const VendorPart = pgTable(
  'VendorPart',
  {
    id: text()
      .$defaultFn(() => createId())
      .primaryKey()
      .notNull(),
    organizationId: text()
      .notNull()
      .references((): AnyPgColumn => Organization.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    partId: text()
      .notNull()
      .references((): AnyPgColumn => Part.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    contactId: text()
      .notNull()
      .references((): AnyPgColumn => Contact.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
    vendorSku: text().notNull(),
    unitPrice: integer(),
    leadTime: integer(),
    minOrderQty: integer(),
    isPreferred: boolean().default(false).notNull(),
    createdAt: timestamp({ precision: 3 }).defaultNow().notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
  },
  (table) => [
    uniqueIndex('VendorPart_partId_contactId_key').using(
      'btree',
      table.partId.asc().nullsLast(),
      table.contactId.asc().nullsLast()
    ),
  ]
)
