// packages/database/src/db/schema/order-refund.ts
// Drizzle table: orderRefund (generated by scripts/split-schema.ts)

import { pgTable, index, text, bigint, timestamp, integer, type AnyPgColumn } from './_shared'

import { Order } from './order'

/** Drizzle table for orderRefund */
export const OrderRefund = pgTable(
  'OrderRefund',
  {
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    id: bigint({ mode: 'number' }).primaryKey().notNull(),
    createdAt: timestamp({ precision: 3 }).notNull(),
    updatedAt: timestamp({ precision: 3 }).notNull(),
    totalRefundedAmount: integer().notNull(),
    currencyCode: text().notNull(),
    // You can use { mode: "bigint" } if numbers are exceeding js number limitations
    orderId: bigint({ mode: 'number' })
      .notNull()
      .references((): AnyPgColumn => Order.id, { onUpdate: 'cascade', onDelete: 'cascade' }),
  },
  (table) => [index('OrderRefund_orderId_idx').using('btree', table.orderId.asc().nullsLast())]
)
