// packages/credentials/src/config/client.ts
// Client-safe exports — pure types only, no server dependencies

export type {
  ConfigVariableDefinition,
  ConfigVariableGroupData,
  ResolvedConfigVariable,
} from './types'
