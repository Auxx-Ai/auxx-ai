// apps/web/src/app/(protected)/app/meetings/loading.tsx

import { LoadingSpinner } from '~/components/global/loading-content'

/**
 * Loading state for Meeting pages.
 */
export default function Loading() {
  return <LoadingSpinner />
}
