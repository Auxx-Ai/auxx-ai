// apps/web/src/app/(protected)/app/parts/layout.tsx

/** Simplified layout for parts page - content is handled by PartsContent component */
export default function PartsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
